.env
node_modules
dist
tmp
# TypeScript build info
*.tsbuildinfo
# SQLite WAL files are always runtime artifacts
*.db-shm
*.db-wal
# Runtime databases (not test fixtures)
server/birdhouse.db
server/data/*.db
# Test-generated databases (not intentional fixtures)
**/test-*.db
server/data/
# Migration planning docs (not for commit)
docs/sessions-to-agents-migration-plan.md
.worktrees/

# Build artifacts
cli-dist/
birdhouse-darwin-*.tar.gz
checksums.txt

# Backup files
*.env.backup
