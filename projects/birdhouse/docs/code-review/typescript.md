# TypeScript Advanced Patterns

This guide covers TypeScript patterns beyond the tsconfig. Read this when working with complex types.

---

## Coming Soon

This guide will cover:
- Generic component patterns
- Type narrowing strategies
- Discriminated unions
- Utility type usage

**For now, see:**
- `src/components/ui/Combobox.tsx` - Good example of generic component with `ComboboxOption<T>`
- TypeScript config: `tsconfig.json` - Strict mode settings
