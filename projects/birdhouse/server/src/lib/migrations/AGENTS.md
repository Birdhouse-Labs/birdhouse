# data.db Migrations

This directory contains the Kysely migration runner and migrations for `data.db`.

Load the **db-migrations** skill for the full guide before doing any migration work:

> "Load the db-migrations skill"

Quick reference:

```bash
# Generate a new migration
bun run data-migration:new <descriptive_name>

# Verify your migration
bun test
```
